<!DOCTYPE html>
<html>
    <head>
        <title>
            Mentee Dashboard
        </title>
        <meta name="viewport" content="width=device-width", initial-scale="1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" >
        <style>
            body {
                background-color: #f9f9fa;
                font-family: sans-serif;
                padding: 20px; <!-- padding defines the space between the content of an element and its border -->
            }

            .dashboard-container {
                max-width: 1000px;<!-- this container won't grow beyond 1000px even if the screen is bigger  -->
                margin: 40px auto; <!-- 40px applies top and bottom margin(space above and below the element) and auto on left and right automatically centers the element horizontally within its container -->
                background-color: #fffffff;
                border-radius: 10px; <!-- border radius property is used to round the corners of an element's border box -->
                box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
                padding: 30px;
            }

            .table-responsive {
                margin-top: 30px;
            }

            .table th, .table td {
                vertical-align: middle;
            }

            .logout-btn-container {
                text-align: right;
                margin-bottom: 20px;
            }
        </style>
    </head>
    <body>
        <div class="dashboard-container">
            <div class="logout-btn-container">
                <a href="/form.html" class="btn btn-outline-danger">Logout</a>
            </div>

            <h1 class="mb-3">Mentee Dashboard</h1>
            <p class="lead">Welcome, {{user.fullname}}!</p>
            <p class="text-muted">Here are your booked sessions with mentors.</p>

            <h2 class="mt-5 mb-3">Your Booked Sessions</h2>
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Mentor Name</th>
                            <th>Mentor Email</th>
                            <th>Start Time</th>
                            <th>End Time</th>
                            <th>Feedback Given</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each sessions}}
                        <tr>
                            <td>{{this.mentor_fullname}}</td>
                            <td>{{this.mentor_email}}</td>
                            <td>{{this.start_time}}</td>
                            <td>{{this.end_time}}</td>
                            <td>{{this.feedback}}</td>
                            <td><span class="badge bg-success">{{this.session_status}}</span></td>
                        </tr>
                        {{else}}
                        <tr>
                            <td colspan="6" class="text-center text-muted py-4">You haven't booked any sessions yet.</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            <div class="text-center mt-4">
                <a href="/available-mentors" class="btn btn-primary">Book New Session</a>
            </div>
        </div>
    </body>
</html>